<div class="hero-card bento-card">
  <div class="hero-top">
    <div class="hero-text">
      <span class="hero-label">// introduction</span>
      <h1 class="hero-name">Adarsh Verma</h1>
      <p class="hero-title">AI-ML student who writes C++</p>
      <p class="hero-bio">Still figuring things out. But the code compiles.</p>
      <div class="hero-badge">
        <span class="typewriter-text" style="--chars: 14;">&gt; open_to_work</span>
      </div>
    </div>

    <div class="photo-container">
      <img src="/me.jpg" alt="Adarsh" class="photo photo-real" />
      <img src="/ProfilePic.jpg" alt="Adarsh avatar" class="photo photo-avatar" />
    </div>
  </div>

  <div class="divider"></div>

  <div class="about">
    <span class="about-label">// about</span>
    <p class="about-text">An AI-ML undergrad who got into programming through competitive coding and hasn't stopped since. I mostly live in C++ and DSA rabbit holes. This portfolio is me trying to build something outside of a code editor for once.</p>
    <div class="about-meta">
      <span class="meta-item">
        <span class="meta-key">college</span>
        <span class="meta-val">GL Bajaj Institute of Technology</span>
      </span>
      <span class="meta-item">
        <span class="meta-key">branch</span>
        <span class="meta-val">AI-ML</span>
      </span>
      <span class="meta-item">
        <span class="meta-key">status</span>
        <span class="meta-val">2nd year</span>
      </span>
    </div>
  </div>
</div>

<style>
  .hero-card {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    height: 100%;
    background-color: var(--burnt-coffee);
    border-radius: 16px;
    padding: 1.5rem;
  }

  .hero-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 1rem;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .hero-label {
    font-size: 0.7rem;
    color: var(--whiskey-sour);
    font-weight: 700;
  }

  .hero-name {
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--champagne);
    line-height: 1;
  }

  .hero-title {
    font-size: 0.9rem;
    color: var(--champagne);
    opacity: 0.9;
  }

  .hero-bio {
    font-size: 0.85rem;
    color: var(--whiskey-sour);
    font-style: italic;
  }

  .hero-badge {
    margin-top: 0.5rem;
    padding: 0.25rem 0.6rem;
    width: fit-content;
    background: var(--honey-garlic); 
    border: 1px solid var(--whiskey-sour);
    border-radius: 4px;
    cursor: pointer;
    overflow: hidden;
  }

  .typewriter-text {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--champagne);
    white-space: nowrap;
    overflow: hidden;
    width: 100%;
  }

  .hero-badge:hover .typewriter-text {
    border-right: 2px solid var(--champagne);
    animation: 
      type-command 0.8s steps(var(--chars)) forwards,
      blink-cursor 0.6s step-end infinite;
  }

  .photo-container {
    position: relative;
    width: 140px;
    height: 140px;
    flex-shrink: 0;
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--honey-garlic);
    transition: border-color 0.3s ease;
  }

  .photo-container:hover {
    border-color: var(--champagne);
  }

  .photo {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: opacity 0.4s ease;
    border-radius: 10px;
  }

  .photo-avatar {
    opacity: 0;
  }

  .photo-container:hover .photo-real {
    opacity: 0;
  }

  .photo-container:hover .photo-avatar {
    opacity: 1;
  }

  .divider {
    height: 1px;
    background: var(--honey-garlic);
    opacity: 0.3;
  }

  .about {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .about-label {
    font-size: 0.7rem;
    color: var(--whiskey-sour);
    font-weight: 700;
  }

  .about-text {
    font-size: 0.85rem;
    color: var(--champagne);
    line-height: 1.7;
  }

  .about-meta {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    margin-top: 0.5rem;
  }

  .meta-item {
    display: flex;
    gap: 0.75rem;
    font-size: 0.8rem;
  }

  .meta-key {
    color: var(--whiskey-sour);
    font-weight: 600;
    min-width: 60px;
  }

  .meta-val {
    color: var(--champagne);
    font-weight: 500;
  }

  @media (max-width: 640px) {
    .hero-top {
      flex-direction: column-reverse;
      align-items: center;
      text-align: center;
      gap: 1.25rem;
    }

    .hero-name {
      font-size: 2rem;
    }

    .hero-badge {
      margin: 0.5rem auto 0 auto;
    }

    .meta-item {
      justify-content: center;
    }
  }
</style>